#!/bin/bash
# MyBinder用のセットアップスクリプト

# Jupyter widgetsの有効化
jupyter nbextension enable --py widgetsnbextension --sys-prefix

# JupyterLab widgetsの有効化（JupyterLab 3.x用）
jupyter labextension list

echo "MyBinder環境のセットアップが完了しました。"